<UserControl x:Class="Board.UI.View.BoardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Board.UI.View" 
        mc:Ignorable="d"
        Background="RoyalBlue"
        MinHeight="600" 
        MinWidth="600"
        Height="600" 
        Width="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row ="0" 
            FontSize="36"
            FontWeight="Bold"
            HorizontalAlignment="Center"
            Text="Connect Four"/>

        <TextBlock 
            Grid.Row ="1" 
            FontSize="18"
            HorizontalAlignment="Center"
            Text="{Binding ActivePlayerString, StringFormat='Active Player: {0}'}"/>

        <local:TokenView Grid.Row="2"/>

    </Grid>
</UserControl>
