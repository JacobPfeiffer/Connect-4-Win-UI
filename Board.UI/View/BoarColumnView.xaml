<UserControl x:Class="Board.UI.View.BoardColumnView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="using:Board.UI.View" 
      xmlns:viewmodel="using:Board.UI.ViewModel" 
      mc:Ignorable="d" 
      x:Name="BoardColumnViewRoot"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Button
            Background="Transparent"
            BorderThickness="0"
            BorderBrush="Transparent"
            Command="{Binding PlaceTokenCommand}">
            <ItemsControl 
                Background="#FF708090"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                ItemsSource="{Binding TokensInColumn}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" Spacing="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse 
                            Width="60" 
                            Height="60" 
                            Fill="{Binding TokenBrush}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Button>
    </Grid>
</UserControl>
